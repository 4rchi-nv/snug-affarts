<section class="section hero">
  <div class="container hero__grid">
    <div>
      <p class="hero__label">Homepage Skeleton</p>
      <h1 class="section__title">Проектная комплектация коммерческих интерьеров</h1>
      <p class="section__description">
        <!-- TODO: insert real headline/subheadline from approved mockup -->
        Temporary hero text to outline spacing, hierarchy and CTA placement.
      </p>
      <a class="hero__cta" href="#">Оставить заявку</a>
    </div>
    <div class="placeholder-card hero__media" aria-hidden="true">Hero image / slider placeholder</div>
  </div>
</section>

<style>
  .hero__grid {
    display: grid;
    grid-template-columns: 1.15fr 1fr;
    gap: 1.5rem;
    align-items: center;
  }

  .hero__label {
    margin: 0 0 0.75rem;
    color: var(--muted);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    font-size: 0.75rem;
  }

  .hero__cta {
    display: inline-block;
    margin-top: 1.25rem;
    padding: 0.75rem 1rem;
    border-radius: 10px;
    background: var(--accent);
    color: #fff;
    text-decoration: none;
    font-weight: 600;
  }

  .hero__media {
    min-height: 280px;
    display: grid;
    place-items: center;
    color: var(--muted);
  }

  @media (max-width: 900px) {
    .hero__grid {
      grid-template-columns: 1fr;
    }
  }
</style>
